// Copyright 2026 shubhan-yenuganti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSlotsDomainOwnershipIdentifiersWebAppsGetSlotCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:   "web-apps-get-slot <resourceGroupName> <name> <domainOwnershipIdentifierName> <slot> <subscriptionId>",
		Aliases: []string{"get"},
		Short: "Get domain ownership identifier for web app.",
		Example: "  functions-pp-cli slots domain-ownership-identifiers web-apps-get-slot example-resource example-resource example-resource example-value example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/slots/{slot}/domainOwnershipIdentifiers/{domainOwnershipIdentifierName}"
			path = replacePathParam(path, "resourceGroupName", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("name is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "name"))
			}
			path = replacePathParam(path, "name", args[1])
			if len(args) < 3 {
				return usageErr(fmt.Errorf("domainOwnershipIdentifierName is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "domainOwnershipIdentifierName"))
			}
			path = replacePathParam(path, "domainOwnershipIdentifierName", args[2])
			if len(args) < 4 {
				return usageErr(fmt.Errorf("slot is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "slot"))
			}
			path = replacePathParam(path, "slot", args[3])
			if len(args) < 5 {
				return usageErr(fmt.Errorf("subscriptionId is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "subscriptionId"))
			}
			path = replacePathParam(path, "subscriptionId", args[4])
			params := map[string]string{}
			data, prov, err := resolveRead(c, flags, "domain-ownership-identifiers", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Print provenance to stderr for human-facing output
			{
				var countItems []json.RawMessage
				_ = json.Unmarshal(data, &countItems)
				printProvenance(cmd, len(countItems), prov)
			}
			// For JSON output, wrap with provenance envelope before passing through flags
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			// For all other output modes (table, csv, plain, quiet), use the standard pipeline
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}

	return cmd
}
