// Copyright 2026 shubhan-yenuganti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newSlotsPromotedCmd(flags *rootFlags) *cobra.Command {

	cmd := &cobra.Command{
		Use:   "slots <resourceGroupName> <name> <slot> <subscriptionId>",
		Short: "Gets the details of a web, mobile, or API app.",
		Long:  "Shortcut for 'slots web-apps-get'. Gets the details of a web, mobile, or API app.",
		Example: "  functions-pp-cli slots",
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/subscriptions/{subscriptionId}/resourceGroups/{resourceGroupName}/providers/Microsoft.Web/sites/{name}/slots/{slot}"
			if len(args) < 1 {
				return usageErr(fmt.Errorf("resourceGroupName is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "resourceGroupName"))
			}
			path = replacePathParam(path, "resourceGroupName", args[0])
			if len(args) < 2 {
				return usageErr(fmt.Errorf("name is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "name"))
			}
			path = replacePathParam(path, "name", args[1])
			if len(args) < 3 {
				return usageErr(fmt.Errorf("slot is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "slot"))
			}
			path = replacePathParam(path, "slot", args[2])
			if len(args) < 4 {
				return usageErr(fmt.Errorf("subscriptionId is required\nUsage: %s %s <%s>", cmd.Root().Name(), cmd.CommandPath(), "subscriptionId"))
			}
			path = replacePathParam(path, "subscriptionId", args[3])
			params := map[string]string{}
			data, prov, err := resolveRead(c, flags, "slots", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}

	// Wire sibling endpoints and sub-resources as subcommands
	cmd.AddCommand(newSlotsWebAppsCreateOrUpdateCmd(flags))
	cmd.AddCommand(newSlotsWebAppsDeleteCmd(flags))
	cmd.AddCommand(newSlotsWebAppsListCmd(flags))
	cmd.AddCommand(newSlotsWebAppsUpdateCmd(flags))
	cmd.AddCommand(newSlotsAnalyzeCustomHostnameWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsApplySlotConfigWebAppsApplySlotConfigurationSlotCmd(flags))
	cmd.AddCommand(newSlotsBackupWebAppsSlotCmd(flags))
	{
		sub := newSlotsBackupsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsConfigCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsContainerlogsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsContinuouswebjobsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsDeploymentsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsDiscoverbackupWebAppsDiscoverBackupSlotCmd(flags))
	{
		sub := newSlotsDomainOwnershipIdentifiersCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsExtensionsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsFunctionsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsHostNameBindingsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsHybridConnectionNamespacesCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsHybridConnectionRelaysWebAppsListHybridConnectionsSlotCmd(flags))
	{
		sub := newSlotsHybridconnectionCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsInstancesCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsIscloneableWebAppsIsCloneableSlotCmd(flags))
	cmd.AddCommand(newSlotsListsyncfunctiontriggerstatusWebAppsListSyncFunctionTriggersSlotCmd(flags))
	cmd.AddCommand(newSlotsMetricdefinitionsWebAppsListMetricDefinitionsSlotCmd(flags))
	cmd.AddCommand(newSlotsMetricsWebAppsListSlotCmd(flags))
	cmd.AddCommand(newSlotsMigratemysqlWebAppsGetMigrateMySqlStatusSlotCmd(flags))
	{
		sub := newSlotsNetworkConfigCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsNetworkFeaturesWebAppsListSlotCmd(flags))
	{
		sub := newSlotsNetworkTraceCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsNetworkTracesCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsNewpasswordWebAppsGenerateNewSitePublishingPasswordSlotCmd(flags))
	cmd.AddCommand(newSlotsPerfcountersWebAppsListPerfMonCountersSlotCmd(flags))
	cmd.AddCommand(newSlotsPhploggingWebAppsGetSitePhpErrorLogFlagSlotCmd(flags))
	{
		sub := newSlotsPremieraddonsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsPrivateAccessCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsProcessesCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsPublicCertificatesCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsPublishxmlWebAppsListPublishingProfileXmlWithSecretsSlotCmd(flags))
	cmd.AddCommand(newSlotsResetSlotConfigWebAppsResetSlotConfigurationSlotCmd(flags))
	cmd.AddCommand(newSlotsRestartWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsRestoreFromBackupBlobWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsRestoreFromDeletedAppWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsRestoreSnapshotWebAppsSlotCmd(flags))
	{
		sub := newSlotsSiteextensionsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsSlotsdiffsWebAppsListSlotDifferencesSlotCmd(flags))
	cmd.AddCommand(newSlotsSlotsswapWebAppsSwapSlotSlotCmd(flags))
	cmd.AddCommand(newSlotsSnapshotsWebAppsListSlotCmd(flags))
	cmd.AddCommand(newSlotsSnapshotsdrWebAppsListSnapshotsFromDrsecondarySlotCmd(flags))
	{
		sub := newSlotsSourcecontrolsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsStartWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsStartNetworkTraceWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsStopWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsStopNetworkTraceWebAppsSlotCmd(flags))
	cmd.AddCommand(newSlotsSyncWebAppsRepositorySlotCmd(flags))
	cmd.AddCommand(newSlotsSyncfunctiontriggersWebAppsSyncFunctionTriggersSlotCmd(flags))
	{
		sub := newSlotsTriggeredwebjobsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	cmd.AddCommand(newSlotsUsagesWebAppsListSlotCmd(flags))
	{
		sub := newSlotsVirtualNetworkConnectionsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}
	{
		sub := newSlotsWebjobsCmd(flags)
		sub.Hidden = false // unhide: the raw parent is hidden but these are useful under the promoted command
		cmd.AddCommand(sub)
	}

	return cmd
}
