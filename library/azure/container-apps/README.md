# Containerapps Client CLI



## Install

### Go

```
go install github.com/mvanhorn/printing-press-library/library/other/container-apps-pp-cli/cmd/container-apps-pp-cli@latest
```

### Binary

Download from [Releases](https://github.com/mvanhorn/printing-press-library/releases).

## Quick Start

### 1. Install

See [Install](#install) above.

### 2. Set Up Credentials

Get your access token from your API provider's developer portal, then store it:

```bash
container-apps-pp-cli auth set-token YOUR_TOKEN_HERE
```

Or set it via environment variable:

```bash
export CONTAINERAPPS_CLIENT_TOKEN="your-token-here"
```

### 3. Verify Setup

```bash
container-apps-pp-cli doctor
```

This checks your configuration and credentials.

### 4. Try Your First Command

```bash
container-apps-pp-cli providers list
```

## Usage

<!-- HELP_OUTPUT -->

## Commands

### providers

Manage providers

- **`container-apps-pp-cli providers container-apps-list-by-subscription`** - Get the Container Apps in a given subscription.

### resource-groups

Manage resource groups



## Output Formats

```bash
# Human-readable table (default in terminal, JSON when piped)
container-apps-pp-cli providers list

# JSON for scripting and agents
container-apps-pp-cli providers list --json

# Filter to specific fields
container-apps-pp-cli providers list --json --select id,name,status

# Dry run — show the request without sending
container-apps-pp-cli providers list --dry-run

# Agent mode — JSON + compact + no prompts in one flag
container-apps-pp-cli providers list --agent
```

## Agent Usage

This CLI is designed for AI agent consumption:

- **Non-interactive** - never prompts, every input is a flag
- **Pipeable** - `--json` output to stdout, errors to stderr
- **Filterable** - `--select id,name` returns only fields you need
- **Previewable** - `--dry-run` shows the request without sending
- **Retryable** - creates return "already exists" on retry, deletes return "already deleted"
- **Confirmable** - `--yes` for explicit confirmation of destructive actions
- **Piped input** - `echo '{"key":"value"}' | container-apps-pp-cli <resource> create --stdin`
- **Cacheable** - GET responses cached for 5 minutes, bypass with `--no-cache`
- **Agent-safe by default** - no colors or formatting unless `--human-friendly` is set
- **Progress events** - paginated commands emit NDJSON events to stderr in default mode

Exit codes: `0` success, `2` usage error, `3` not found, `4` auth error, `5` API error, `7` rate limited, `10` config error.

## Use as MCP Server

This CLI ships a companion MCP server for use with Claude Desktop, Cursor, and other MCP-compatible tools.

### Claude Code

```bash
claude mcp add container-apps container-apps-pp-mcp -e CONTAINERAPPS_CLIENT_TOKEN=<your-token>
```

### Claude Desktop

Add to your Claude Desktop config (`~/Library/Application Support/Claude/claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "container-apps": {
      "command": "container-apps-pp-mcp",
      "env": {
        "CONTAINERAPPS_CLIENT_TOKEN": "<your-key>"
      }
    }
  }
}
```

## Cookbook

Common workflows and recipes:

```bash
# List resources as JSON for scripting
container-apps-pp-cli providers list --json

# Filter to specific fields
container-apps-pp-cli providers list --json --select id,name,status

# Dry run to preview the request
container-apps-pp-cli providers list --dry-run

# Sync data locally for offline search
container-apps-pp-cli sync

# Search synced data
container-apps-pp-cli search "query"

# Export for backup
container-apps-pp-cli export --format jsonl > backup.jsonl
```

## Health Check

```bash
container-apps-pp-cli doctor
```

<!-- DOCTOR_OUTPUT -->

## Configuration

Config file: `~/.config/container-apps-pp-cli/config.toml`

Environment variables:
- `CONTAINERAPPS_CLIENT_TOKEN`

## Troubleshooting

**Authentication errors (exit code 4)**
- Run `container-apps-pp-cli doctor` to check credentials
- Verify the environment variable is set: `echo $CONTAINERAPPS_CLIENT_TOKEN`

**Not found errors (exit code 3)**
- Check the resource ID is correct
- Run the `list` command to see available items

**Rate limit errors (exit code 7)**
- The CLI auto-retries with exponential backoff
- If persistent, wait a few minutes and try again

---

Generated by [CLI Printing Press](https://github.com/mvanhorn/cli-printing-press)
