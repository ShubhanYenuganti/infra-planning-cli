// Copyright 2026 shubhan-yenuganti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newV20150331UpdateFunctionCodeCmd(flags *rootFlags) *cobra.Command {
	var bodyArchitectures string
	var bodyDryRun bool
	var bodyImageUri string
	var bodyPublish bool
	var bodyRevisionId string
	var bodyS3Bucket string
	var bodyS3Key string
	var bodyS3ObjectVersion string
	var bodyZipFile string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "update-function-code <FunctionName>",
		Short: "<p>Updates a Lambda function's code. If code signing is enabled for the function, the code package must be signed by...",
		Example: "  aws-lambda-pp-cli 2015-03-31 update-function-code example-resource",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2015-03-31/functions/{FunctionName}/code"
			path = replacePathParam(path, "FunctionName", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyArchitectures != "" {
					body["Architectures"] = bodyArchitectures
				}
				if bodyDryRun != false {
					body["DryRun"] = bodyDryRun
				}
				if bodyImageUri != "" {
					body["ImageUri"] = bodyImageUri
				}
				if bodyPublish != false {
					body["Publish"] = bodyPublish
				}
				if bodyRevisionId != "" {
					body["RevisionId"] = bodyRevisionId
				}
				if bodyS3Bucket != "" {
					body["S3Bucket"] = bodyS3Bucket
				}
				if bodyS3Key != "" {
					body["S3Key"] = bodyS3Key
				}
				if bodyS3ObjectVersion != "" {
					body["S3ObjectVersion"] = bodyS3ObjectVersion
				}
				if bodyZipFile != "" {
					body["ZipFile"] = bodyZipFile
				}
			}
			data, statusCode, err := c.Put(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				envelope := map[string]any{
					"action":   "put",
					"resource": "2015-03-31",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyArchitectures, "architectures", "", "The instruction set architecture that the function supports. Enter a string array with one of the valid values...")
	cmd.Flags().BoolVar(&bodyDryRun, "dry-run", false, "Set to true to validate the request parameters and access permissions without modifying the function code.")
	cmd.Flags().StringVar(&bodyImageUri, "image-uri", "", "URI of a container image in the Amazon ECR registry. Do not use for a function defined with a .zip file archive.")
	cmd.Flags().BoolVar(&bodyPublish, "publish", false, "Set to true to publish a new version of the function after updating the code. This has the same effect as calling...")
	cmd.Flags().StringVar(&bodyRevisionId, "revision-id", "", "Update the function only if the revision ID matches the ID that's specified. Use this option to avoid modifying a...")
	cmd.Flags().StringVar(&bodyS3Bucket, "s3-bucket", "", "An Amazon S3 bucket in the same Amazon Web Services Region as your function. The bucket can be in a different Amazon...")
	cmd.Flags().StringVar(&bodyS3Key, "s3-key", "", "The Amazon S3 key of the deployment package. Use only with a function defined with a .zip file archive deployment...")
	cmd.Flags().StringVar(&bodyS3ObjectVersion, "s3-object-version", "", "For versioned objects, the version of the deployment package object to use.")
	cmd.Flags().StringVar(&bodyZipFile, "zip-file", "", "The base64-encoded contents of the deployment package. Amazon Web Services SDK and CLI clients handle the encoding...")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
