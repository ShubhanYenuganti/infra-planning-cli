// Copyright 2026 shubhan-yenuganti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"os"

	"github.com/spf13/cobra"
)

func newV20181031PromotedCmd(flags *rootFlags) *cobra.Command {
	var flagCompatibleRuntime string
	var flagMarker string
	var flagMaxItems int
	var flagCompatibleArchitecture string

	cmd := &cobra.Command{
		Use:   "2018-10-31",
		Short: "Lists <a href='https://docs.aws.amazon.com/lambda/latest/dg/invocation-layers.html'>Lambda layers</a> and shows...",
		Long:  "Shortcut for '2018-10-31 list-layers'. Lists <a href='https://docs.aws.amazon.com/lambda/latest/dg/invocation-layers.html'>Lambda layers</a> and shows...",
		Example: "  aws-lambda-pp-cli 2018-10-31",
		RunE: func(cmd *cobra.Command, args []string) error {
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2018-10-31/layers"
			params := map[string]string{}
			if flagCompatibleRuntime != "" {
				params["CompatibleRuntime"] = fmt.Sprintf("%v", flagCompatibleRuntime)
			}
			if flagMarker != "" {
				params["Marker"] = fmt.Sprintf("%v", flagMarker)
			}
			if flagMaxItems != 0 {
				params["MaxItems"] = fmt.Sprintf("%v", flagMaxItems)
			}
			if flagCompatibleArchitecture != "" {
				params["CompatibleArchitecture"] = fmt.Sprintf("%v", flagCompatibleArchitecture)
			}
			data, prov, err := resolveRead(c, flags, "2018-10-31", false, path, params)
			if err != nil {
				return classifyAPIError(err)
			}
			// Unwrap API response envelopes (e.g. {"status":"success","data":[...]})
			// so output helpers see the inner data, not the wrapper.
			data = extractResponseData(data)

			// Print provenance to stderr
			{
				var countItems []json.RawMessage
				if json.Unmarshal(data, &countItems) != nil {
					// Single object, not an array
					countItems = []json.RawMessage{data}
				}
				printProvenance(cmd, len(countItems), prov)
			}
			// CSV bypasses JSON pipe path so --csv works when piped
			if flags.csv {
				return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
			}
			// For JSON output, wrap with provenance envelope
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				wrapped, wrapErr := wrapWithProvenance(filtered, prov)
				if wrapErr != nil {
					return wrapErr
				}
				return printOutput(cmd.OutOrStdout(), wrapped, true)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						return err
					}
					if len(items) >= 25 {
						fmt.Fprintf(os.Stderr, "\nShowing %d results. To narrow: add --limit, --json --select, or filter flags.\n", len(items))
					}
					return nil
				}
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagCompatibleRuntime, "compatible-runtime", "", "A runtime identifier. For example, <code>go1.x</code>.")
	cmd.Flags().StringVar(&flagMarker, "marker", "", "A pagination token returned by a previous call.")
	cmd.Flags().IntVar(&flagMaxItems, "max-items", 0, "The maximum number of layers to return.")
	cmd.Flags().StringVar(&flagCompatibleArchitecture, "compatible-architecture", "", "The compatible <a href='https://docs.aws.amazon.com/lambda/latest/dg/foundation-arch.html'>instruction set...")

	// Wire sibling endpoints and sub-resources as subcommands
	cmd.AddCommand(newV20181031AddLayerVersionPermissionCmd(flags))
	cmd.AddCommand(newV20181031DeleteLayerVersionCmd(flags))
	cmd.AddCommand(newV20181031GetLayerVersionCmd(flags))
	cmd.AddCommand(newV20181031GetLayerVersionByArnCmd(flags))
	cmd.AddCommand(newV20181031GetLayerVersionPolicyCmd(flags))
	cmd.AddCommand(newV20181031ListLayerVersionsCmd(flags))
	cmd.AddCommand(newV20181031PublishLayerVersionCmd(flags))
	cmd.AddCommand(newV20181031RemoveLayerVersionPermissionCmd(flags))

	return cmd
}
