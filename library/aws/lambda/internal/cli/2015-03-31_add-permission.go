// Copyright 2026 shubhan-yenuganti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newV20150331AddPermissionCmd(flags *rootFlags) *cobra.Command {
	var flagQualifier string
	var bodyAction string
	var bodyEventSourceToken string
	var bodyFunctionUrlAuthType string
	var bodyPrincipal string
	var bodyPrincipalOrgID string
	var bodyRevisionId string
	var bodySourceAccount string
	var bodySourceArn string
	var bodyStatementId string
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "add-permission <FunctionName>",
		Aliases: []string{"create"},
		Short: "<p>Grants an Amazon Web Service, Amazon Web Services account, or Amazon Web Services organization permission to use...",
		Example: "  aws-lambda-pp-cli 2015-03-31 add-permission example-resource --Action example-value",
		RunE: func(cmd *cobra.Command, args []string) error {
			if len(args) == 0 {
				return cmd.Help()
			}
			if !stdinBody {
				if !cmd.Flags().Changed("action") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "action")
				}
				if !cmd.Flags().Changed("principal") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "principal")
				}
				if !cmd.Flags().Changed("statement-id") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "statement-id")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2015-03-31/functions/{FunctionName}/policy"
			path = replacePathParam(path, "FunctionName", args[0])
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyAction != "" {
					body["Action"] = bodyAction
				}
				if bodyEventSourceToken != "" {
					body["EventSourceToken"] = bodyEventSourceToken
				}
				if bodyFunctionUrlAuthType != "" {
					body["FunctionUrlAuthType"] = bodyFunctionUrlAuthType
				}
				if bodyPrincipal != "" {
					body["Principal"] = bodyPrincipal
				}
				if bodyPrincipalOrgID != "" {
					body["PrincipalOrgID"] = bodyPrincipalOrgID
				}
				if bodyRevisionId != "" {
					body["RevisionId"] = bodyRevisionId
				}
				if bodySourceAccount != "" {
					body["SourceAccount"] = bodySourceAccount
				}
				if bodySourceArn != "" {
					body["SourceArn"] = bodySourceArn
				}
				if bodyStatementId != "" {
					body["StatementId"] = bodyStatementId
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "2015-03-31",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&flagQualifier, "qualifier", "", "Specify a version or alias to add permissions to a published version of the function.")
	cmd.Flags().StringVar(&bodyAction, "action", "", "The action that the principal can use on the function. For example, <code>lambda:InvokeFunction</code> or...")
	cmd.Flags().StringVar(&bodyEventSourceToken, "event-source-token", "", "For Alexa Smart Home functions, a token that the invoker must supply.")
	cmd.Flags().StringVar(&bodyFunctionUrlAuthType, "function-url-auth-type", "", "The type of authentication that your function URL uses. Set to <code>AWS_IAM</code> if you want to restrict access...")
	cmd.Flags().StringVar(&bodyPrincipal, "principal", "", "The Amazon Web Service or Amazon Web Services account that invokes the function. If you specify a service, use...")
	cmd.Flags().StringVar(&bodyPrincipalOrgID, "principal-org-id", "", "The identifier for your organization in Organizations. Use this to grant permissions to all the Amazon Web Services...")
	cmd.Flags().StringVar(&bodyRevisionId, "revision-id", "", "Update the policy only if the revision ID matches the ID that's specified. Use this option to avoid modifying a...")
	cmd.Flags().StringVar(&bodySourceAccount, "source-account", "", "For Amazon Web Service, the ID of the Amazon Web Services account that owns the resource. Use this together with...")
	cmd.Flags().StringVar(&bodySourceArn, "source-arn", "", "<p>For Amazon Web Services, the ARN of the Amazon Web Services resource that invokes the function. For example, an...")
	cmd.Flags().StringVar(&bodyStatementId, "statement-id", "", "A statement identifier that differentiates the statement from others in the same policy.")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
