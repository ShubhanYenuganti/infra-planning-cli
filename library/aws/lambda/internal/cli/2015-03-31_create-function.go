// Copyright 2026 shubhan-yenuganti. Licensed under Apache-2.0. See LICENSE.
// Generated by CLI Printing Press (https://github.com/mvanhorn/cli-printing-press). DO NOT EDIT.

package cli

import (
	"encoding/json"
	"fmt"
	"io"
	"os"

	"github.com/spf13/cobra"
)

func newV20150331CreateFunctionCmd(flags *rootFlags) *cobra.Command {
	var bodyArchitectures string
	var bodyCodeSigningConfigArn string
	var bodyDescription string
	var bodyFunctionName string
	var bodyHandler string
	var bodyKMSKeyArn string
	var bodyLayers string
	var bodyMemorySize int
	var bodyPackageType string
	var bodyPublish bool
	var bodyRole string
	var bodyRuntime string
	var bodyTimeout int
	var stdinBody bool

	cmd := &cobra.Command{
		Use:   "create-function",
		Short: "<p>Creates a Lambda function. To create a function, you need a <a...",
		Example: "  aws-lambda-pp-cli 2015-03-31 create-function --FunctionName example-resource",
		RunE: func(cmd *cobra.Command, args []string) error {
			if !stdinBody {
				if !cmd.Flags().Changed("function-name") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "function-name")
				}
				if !cmd.Flags().Changed("role") && !flags.dryRun {
					return fmt.Errorf("required flag \"%s\" not set", "role")
				}
			}
			c, err := flags.newClient()
			if err != nil {
				return err
			}

			path := "/2015-03-31/functions"
			var body map[string]any
			if stdinBody {
				stdinData, err := io.ReadAll(os.Stdin)
				if err != nil {
					return fmt.Errorf("reading stdin: %w", err)
				}
				var jsonBody map[string]any
				if err := json.Unmarshal(stdinData, &jsonBody); err != nil {
					return fmt.Errorf("parsing stdin JSON: %w", err)
				}
				body = jsonBody
			} else {
				body = map[string]any{}
				if bodyArchitectures != "" {
					body["Architectures"] = bodyArchitectures
				}
				if bodyCodeSigningConfigArn != "" {
					body["CodeSigningConfigArn"] = bodyCodeSigningConfigArn
				}
				if bodyDescription != "" {
					body["Description"] = bodyDescription
				}
				if bodyFunctionName != "" {
					body["FunctionName"] = bodyFunctionName
				}
				if bodyHandler != "" {
					body["Handler"] = bodyHandler
				}
				if bodyKMSKeyArn != "" {
					body["KMSKeyArn"] = bodyKMSKeyArn
				}
				if bodyLayers != "" {
					body["Layers"] = bodyLayers
				}
				if bodyMemorySize != 0 {
					body["MemorySize"] = bodyMemorySize
				}
				if bodyPackageType != "" {
					body["PackageType"] = bodyPackageType
				}
				if bodyPublish != false {
					body["Publish"] = bodyPublish
				}
				if bodyRole != "" {
					body["Role"] = bodyRole
				}
				if bodyRuntime != "" {
					body["Runtime"] = bodyRuntime
				}
				if bodyTimeout != 0 {
					body["Timeout"] = bodyTimeout
				}
			}
			data, statusCode, err := c.Post(path, body)
			if err != nil {
				return classifyAPIError(err)
			}
			if wantsHumanTable(cmd.OutOrStdout(), flags) {
				// Check if response contains an array (directly or wrapped in "data")
				var items []map[string]any
				if json.Unmarshal(data, &items) == nil && len(items) > 0 {
					if err := printAutoTable(cmd.OutOrStdout(), items); err != nil {
						fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
					} else {
						return nil
					}
				} else {
					var wrapped struct{ Data []map[string]any `json:"data"` }
					if json.Unmarshal(data, &wrapped) == nil && len(wrapped.Data) > 0 {
						if err := printAutoTable(cmd.OutOrStdout(), wrapped.Data); err != nil {
							fmt.Fprintf(os.Stderr, "warning: table rendering failed, falling back to JSON: %v\n", err)
						} else {
							return nil
						}
					}
				}
			}
			if flags.asJSON || !isTerminal(cmd.OutOrStdout()) {
				if flags.quiet {
					return nil
				}
				// Apply --compact and --select to the API response before wrapping
				filtered := data
				if flags.compact {
					filtered = compactFields(filtered)
				}
				if flags.selectFields != "" {
					filtered = filterFields(filtered, flags.selectFields)
				}
				envelope := map[string]any{
					"action":   "post",
					"resource": "2015-03-31",
					"path":     path,
					"status":   statusCode,
					"success":  statusCode >= 200 && statusCode < 300,
				}
				if flags.dryRun {
					envelope["dry_run"] = true
					envelope["status"] = 0
					envelope["success"] = false
				}
				if len(filtered) > 0 {
					var parsed any
					if err := json.Unmarshal(filtered, &parsed); err == nil {
						envelope["data"] = parsed
					}
				}
				envelopeJSON, err := json.Marshal(envelope)
				if err != nil {
					return err
				}
				return printOutput(cmd.OutOrStdout(), json.RawMessage(envelopeJSON), true)
			}
			return printOutputWithFlags(cmd.OutOrStdout(), data, flags)
		},
	}
	cmd.Flags().StringVar(&bodyArchitectures, "architectures", "", "The instruction set architecture that the function supports. Enter a string array with one of the valid values...")
	cmd.Flags().StringVar(&bodyCodeSigningConfigArn, "code-signing-config-arn", "", "To enable code signing for this function, specify the ARN of a code-signing configuration. A code-signing...")
	cmd.Flags().StringVar(&bodyDescription, "description", "", "A description of the function.")
	cmd.Flags().StringVar(&bodyFunctionName, "function-name", "", "<p>The name of the Lambda function.</p> <p class='title'> <b>Name formats</b> </p> <ul> <li> <p> <b>Function...")
	cmd.Flags().StringVar(&bodyHandler, "handler", "", "The name of the method within your code that Lambda calls to run your function. Handler is required if the...")
	cmd.Flags().StringVar(&bodyKMSKeyArn, "kms-key-arn", "", "The ARN of the Key Management Service (KMS) customer managed key that's used to encrypt your function's <a...")
	cmd.Flags().StringVar(&bodyLayers, "layers", "", "A list of <a href='https://docs.aws.amazon.com/lambda/latest/dg/configuration-layers.html'>function layers</a> to...")
	cmd.Flags().IntVar(&bodyMemorySize, "memory-size", 0, "The amount of <a href='https://docs.aws.amazon.com/lambda/latest/dg/configuration-function-common.html#configuration-...")
	cmd.Flags().StringVar(&bodyPackageType, "package-type", "", "The type of deployment package. Set to <code>Image</code> for container image and set to <code>Zip</code> for .zip...")
	cmd.Flags().BoolVar(&bodyPublish, "publish", false, "Set to true to publish the first version of the function during creation.")
	cmd.Flags().StringVar(&bodyRole, "role", "", "The Amazon Resource Name (ARN) of the function's execution role.")
	cmd.Flags().StringVar(&bodyRuntime, "runtime", "", "<p>The identifier of the function's <a href='https://docs.aws.amazon.com/lambda/latest/dg/lambda-runtimes.html'>runti...")
	cmd.Flags().IntVar(&bodyTimeout, "timeout", 0, "The amount of time (in seconds) that Lambda allows a function to run before stopping it. The default is 3 seconds....")
	cmd.Flags().BoolVar(&stdinBody, "stdin", false, "Read request body as JSON from stdin")

	return cmd
}
