# Sprint v1 — Serverless + Managed Containers (SUBSTRATE SPRINT)

## Goal

Ship 6 CLIs (Lambda, Cloud Functions, Azure Functions, App Runner, Cloud Run,
Container Apps) AND build the regen-ready substrate that all future sprints use.

## Substrate deliverables (one-time, v1 only)

- [x] Repo rename `infra-planning-cli` → `infra-press` *(local: pending manual GitHub UI rename)*
- [x] Old code moved to `old-infra-cli` worktree
- [x] `docs/conventions.md` written (the spec the linter enforces)
- [x] `tools/lint-conventions/` implemented (5 checks, 11 tests passing)
- [x] `tests/golden/` orchestrator + initial checks implemented (4 checks, tests passing)
- [x] `scripts/install.sh`, `scripts/doctor-{aws,gcp,azure}.sh` implemented
- [x] Root `AGENTS.md` authored
- [x] `catalog.yaml` v1 schema defined
- [x] CI workflows (`lint.yml`, `per-cli-tests.yml`, `golden.yml`, `release.yml`, `nightly.yml`)
- [x] `pressfile.yaml` schema documented (in docs/conventions.md)
- [x] Patch convention documented (in docs/conventions.md)

## CLIs in v1

| CLI | Cloud | Spec source | Status |
|---|---|---|---|
| cloud-run-admin-pp-cli | GCP | https://run.googleapis.com/$discovery/rest?version=v2 | pending (this plan) |
| apprunner-pp-cli | AWS | (Smithy / SDK model) | pending (follow-on plan) |
| lambda-pp-cli | AWS | (Smithy / SDK model) | pending (follow-on plan) |
| container-apps-pp-cli | Azure | (ARM spec, Microsoft.App) | pending (follow-on plan) |
| functions-pp-cli | Azure | (ARM spec, Microsoft.Web/sites) | pending (follow-on plan) |
| cloud-functions-pp-cli | GCP | https://cloudfunctions.googleapis.com/$discovery/rest?version=v2 | pending (follow-on plan) |

## Press-readiness check

Representative API for the gate: **`cloud-run-admin-pp-cli`** (GCP).

Rationale: mvanhorn already shipped this CLI, so we have a known-good output
to validate our substrate against. If our generated cloud-run-admin doesn't
match the known-good shape (modulo our convention layer), the substrate has
a bug.

### Result: BLOCKED (2026-05-20)

Attempted `printing-press generate --spec 'https://run.googleapis.com/$discovery/rest?version=v2'`.
Press fails with: `validation: base_url is required`.

**Root cause:** GCP Discovery documents are not OpenAPI 3.0. They use a Google-specific
schema (`rootUrl` + `servicePath` instead of `servers[]`, `baseUrl` in camelCase, etc.).
The press expects OpenAPI 3.0 input.

**Catalog check:** `printing-press catalog list` returns 11 SaaS APIs (stytch,
digitalocean, github, launchdarkly, postman-explore, petstore, sendgrid, sentry,
plaid, square, stripe). **Zero GCP / AWS / Azure entries.** Confirms cloud-infra
APIs require manual spec resolution per CLI; press does not ship adapters for
Discovery, Smithy, or ARM.

**Options to unblock Phase 6 (decide before next sprint pass):**

1. **Find published OpenAPI specs for Cloud Run.** Google's `googleapis/googleapis`
   repo emits OpenAPI specs alongside protos for some services. Verify Cloud Run
   has one and point `--spec` at the raw URL.
2. **Use `--docs` mode instead of `--spec`.** Press supports doc-scraping
   (`--docs https://cloud.google.com/run/docs/reference/rest/v2`). Output quality
   may be lower but doesn't require spec format negotiation.
3. **Manual Discovery→OpenAPI conversion.** Write a small tool (or use existing
   `gapic-generator-typescript`-adjacent tooling) to convert Discovery docs to
   OpenAPI 3.0, then feed to press.
4. **Patch press upstream.** Add Discovery format support to mvanhorn's press.
   Largest scope but unblocks every GCP service for v1.1+ sprints.

**Recommendation:** option 2 first (`--docs` mode) for cloud-run; if quality is
poor, fall back to option 1 (search for OpenAPI specs). Option 4 is a v2+ task.

**Substrate status:** Phases 0-5 complete and shippable. CLI generation
(Phase 6) blocked pending spec-format decision above.

## Per-CLI gap notes

(Captured per-CLI during Phase 6+ as scorecards complete.)

## Cross-cloud guidance to add to root AGENTS.md

- (Single-cloud v1; cross-cloud guidance accumulates as v1.1+ sprints add CLIs.)

## Definition of done

- [ ] All substrate deliverables checked off above
- [ ] cloud-run-admin-pp-cli through 8-step pipeline (this plan's Phase 6)
- [ ] Remaining 5 CLIs through 8-step pipeline (follow-on plan)
- [ ] All Layer 1 + Layer 2 tests green
- [ ] catalog.yaml lists all 6 with status: stable
- [ ] Root AGENTS.md updated with all 6 in "When to reach for what" table
- [ ] All 6 CLIs released and binaries published
- [ ] At least one cross-CLI compound recipe documented

## Retro (preliminary — substrate ship, Phase 6 blocked)

### What worked

- Substrate-first approach: linter + golden tests existed before any CLI,
  so when Phase 6 hits a real gap there's no half-baked CLI sitting in
  `library/` violating conventions.
- Per-CLI Go module structure validated with the lint-conventions and
  tests/golden modules — both are independently-versioned Go modules in
  the monorepo, exactly the pattern future CLIs will follow.
- Migration to `old-infra-cli` worktree clean: 58 deleted files, no
  residual cmd/internal/tests dirs on main.
- 5 lint checks + 4 golden checks all green against test fixtures and
  mock binaries respectively. The pattern (Check interface + Registry()
  function) makes adding a new check ~30 LOC.

### What surprised us

- **mvanhorn's press has zero cloud-infra APIs in its catalog.** The
  embedded catalog is 11 SaaS APIs (Stripe, GitHub, Stytch, Sentry, etc.).
  cloud-run-admin in the printing-press-library is a manual one-off, not
  representative of how press handles GCP/AWS/Azure broadly.
- **Press only consumes OpenAPI 3.0.** Doesn't natively parse GCP
  Discovery docs, AWS Smithy, or Azure ARM specs. This means every
  cloud-infra sprint will need a per-cloud spec-format adapter step
  before the press itself runs. Not captured in the design spec; needs
  to be added before v1.1.
- **Go 1.26 brew install was painless.** Spec called this the highest-risk
  open question; took ~30 seconds via `brew install go@1.26`. The risk
  evaporated.

### What we'd change for v1.1

- **Add a "spec resolution" step to the 8-step pipeline** that explicitly
  converts cloud-native spec formats (Discovery / Smithy / ARM) to
  OpenAPI 3.0 before invoking press. This becomes a per-cloud subtool
  (e.g., `tools/discovery-to-openapi/`).
- **Reconsider the press dependency.** If 100% of cloud APIs need a
  pre-conversion step, the press's "absorb gate" and "ecosystem absorb"
  phases (its key differentiators) may be wasted effort for cloud-infra.
  Either: (a) accept that cost for the SQLite/FTS5/conventions output;
  or (b) write a smaller cloud-specific generator that emits the same
  output shape without the press's SaaS-API assumptions.
- **The convention linter is currently text-based** (`strings.Contains`).
  Fine for v1 but a future CLI with creative formatting could pass the
  lint while violating spirit. Consider Go AST-based checks in v1.5+.

### Open questions to resolve before Phase 6 retry

1. Which spec format adapter for Cloud Run? (See "Press-readiness check"
   section above — 4 options.)
2. Does the same blocker apply to AWS (Smithy) and Azure (ARM)? Almost
   certainly yes; investigate before committing v1.1 sprint plan.
3. Should we vendor mvanhorn's printing-press-library `cloud-run-admin`
   output directly as a starting point (rather than re-generating from
   spec), accepting we lose press version pinning for that one CLI?
   This trades regen-readiness for shipping speed on the proof CLI.

### Substrate ship status

**Ready to ship.** All Phases 0-5 complete. Substrate exercises end-to-end
on mock fixtures (5 lint checks + 4 golden checks). The library/ directory
is empty pending Phase 6 unblock. CI workflows, install scripts, and hub
artifacts are in place and parseable. Future sprints can run the 8-step
pipeline against any CLI as soon as the spec-format question is resolved.
